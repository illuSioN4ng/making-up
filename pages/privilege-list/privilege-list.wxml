<view class="container">
    <view wx:for="{{discount}}" wx:key="{{ index }}">
        <view bindtap="seeDetail" wx:key="id" data-id="{{item.id}}">
            <image class="privilege-item" src="{{ item.background_url }}"></image>
        </view>
    </view>
</view>